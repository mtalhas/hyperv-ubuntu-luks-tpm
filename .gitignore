# Local configuration with machine specific paths.
/config.ps1

# Build output. This holds generated secrets, logs and per VM build files.
/out/
/build/

# Generated credentials and keys, wherever they land.
.env
*.env
id_ed25519
id_ed25519.pub
*.key
*.pem

# Filled in answer files that contain the real password and passphrase.
**/autoinstall/user-data
**/autoinstall/meta-data

# Rebuilt install images and console screenshots from failed runs.
*.iso
*.png

# Test output.
test-results.xml

# Windows and editor noise.
Thumbs.db
desktop.ini
*.tmp
NUL
